{
  "name": "ideadraw",
  "version": "0.1.0",
  "description": "A spatial prompt editor and ComfyUI frontend for Ideogram 4 workflows.",
  "license": "MIT",
  "private": false,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/inpaint.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
